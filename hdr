Voici les paramètres à ajouter dans votre fichier `firefox.cfg` pour désactiver la sauvegarde des identifiants et les suggestions :

```javascript
// Désactiver la sauvegarde des mots de passe
lockPref("signon.rememberSignons", false);

// Désactiver les suggestions de noms d'utilisateur dans les champs de connexion
lockPref("signon.autofillForms", false);

// Désactiver la demande de sauvegarde des mots de passe
lockPref("signon.passwordEditCapture.enabled", false);

// Désactiver les suggestions automatiques des champs de formulaire
lockPref("browser.formfill.enable", false);

// Désactiver l'autocomplétion des formulaires
lockPref("signon.autofillForms.autocompleteOff", true);

// Désactiver le gestionnaire de mots de passe intégré
lockPref("signon.management.page.breach-alerts.enabled", false);

// Désactiver les notifications de violation de données pour les mots de passe
lockPref("signon.management.page.breachAlertUrl", "");

// Optionnel : Désactiver complètement l'historique des formulaires
lockPref("browser.formfill.saveHttpsForms", false);
```

**Configuration complète avec vos fichiers JNLP :**

```javascript
// Configuration pour les fichiers JNLP
lockPref("network.protocol-handler.expose.jnlp", false);
lockPref("network.protocol-handler.external.jnlp", true);
lockPref("network.protocol-handler.app.jnlp", "javaws");
lockPref("network.protocol-handler.warn-external.jnlp", false);
lockPref("network.mime.disable_default_plugin_for.application/x-java-jnlp-file", false);

// Désactiver la sauvegarde des identifiants et suggestions
lockPref("signon.rememberSignons", false);
lockPref("signon.autofillForms", false);
lockPref("signon.passwordEditCapture.enabled", false);
lockPref("browser.formfill.enable", false);
lockPref("signon.autofillForms.autocompleteOff", true);
lockPref("signon.management.page.breach-alerts.enabled", false);
lockPref("browser.formfill.saveHttpsForms", false);
```

Après avoir ajouté ces paramètres, redémarrez Firefox. Les utilisateurs ne verront plus :

- Les demandes de sauvegarde de mots de passe
- Les suggestions d’autocomplétion dans les champs de connexion
- L’historique des formulaires précédemment remplis​​​​​​​​​​​​​​​​